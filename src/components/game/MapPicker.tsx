/**
 * @deprecated Import from `@/components/game/main-menu` instead.
 * Thin re-export so existing `MapPicker` imports keep working.
 */
export {
  MainMenu as MapPicker,
  type MainMenuProps as MapPickerProps,
  type OnlineLobbyJoin,
} from './main-menu'
