/** Re-export shared sniper FSM. */
export {
  createSniper,
  resetSniper,
  stepSniper,
  tryFire,
  applyRecoil,
  aimSpread,
  aimSway,
  effectiveLook,
} from '@glint/shared'
export type { FireResult } from '@glint/shared'
